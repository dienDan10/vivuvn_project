import 'package:flutter/material.dart';

class NearbyRestaurantLayout extends StatelessWidget {
  const NearbyRestaurantLayout({super.key});

  @override
  Widget build(final BuildContext context) {
    return Container();
  }
}
