# VivuVN Production Environment Configuration
# 
# INSTRUCTIONS FOR PRODUCTION DEPLOYMENT:
# 1. Copy this file: cp .env.prod.template .env.prod
# 2. Replace ALL placeholder values with secure production credentials
# 3. NEVER use development keys in production!
# 4. Store this file securely and never commit to version control
#
# SECURITY: All values below MUST be changed for production

# ============================================================================
# SQL Server Configuration (PRODUCTION)
# ============================================================================
SA_PASSWORD=CHANGE-TO-EXTREMELY-STRONG-PRODUCTION-PASSWORD-32-CHARS-MIN

# ============================================================================
# Application Configuration  
# ============================================================================
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:80

# ============================================================================
# JWT Authentication (PRODUCTION - GENERATE NEW KEYS!)
# ============================================================================
# Generate: openssl rand -base64 128
# CRITICAL: Must be different from development keys
JWT_SECRET_KEY=GENERATE-A-NEW-128-CHARACTER-PRODUCTION-JWT-SECRET-KEY-DIFFERENT-FROM-DEV
JWT_ISSUER=https://your-production-domain.com/
JWT_AUDIENCE=https://your-production-domain.com/

# ============================================================================
# External API Keys (PRODUCTION KEYS ONLY!)
# ============================================================================

# Brevo Email Service (Production Account)
BREVO_API_KEY=your-production-brevo-api-key-here
BREVO_SENDER_EMAIL=noreply@your-production-domain.com

# Google Services (Production Project)
GOOGLE_MAPS_API_KEY=your-production-google-maps-api-key-here
GOOGLE_OAUTH_CLIENT_ID=your-production-google-oauth-client-id-here

# OpenAI API (Production Account)
OPENAI_API_KEY=your-production-openai-api-key-here

# ============================================================================
# Client Application URLs (PRODUCTION)
# ============================================================================
ADMIN_PANEL_URL=https://admin.your-production-domain.com

# ============================================================================
# PRODUCTION SECURITY CHECKLIST:
# ============================================================================
# ? Changed ALL passwords from development versions
# ? Generated new JWT_SECRET_KEY (different from dev)
# ? Using production API keys (not development keys)
# ? Updated URLs to production domains
# ? Verified all API keys have proper restrictions/allowlists
# ? Database password is 32+ characters
# ? This file is stored securely and NOT in version control
# ? All team members have unique production credentials
# ============================================================================