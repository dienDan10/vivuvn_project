# VivuVN Docker Environment Configuration Template
# 
# INSTRUCTIONS:
# 1. Copy this file: cp .env.template .env
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit the .env file to version control!
#
# SECURITY: All values below are safe placeholders - they won't work if used as-is

# ============================================================================
# SQL Server Configuration
# ============================================================================
SA_PASSWORD=CHANGE-THIS-TO-A-STRONG-DATABASE-PASSWORD

# ============================================================================
# Application Configuration  
# ============================================================================
ASPNETCORE_ENVIRONMENT=Docker
ASPNETCORE_URLS=http://+:80

# ============================================================================
# JWT Authentication (GENERATE NEW SECURE KEY!)
# ============================================================================
# Generate a secure key: openssl rand -base64 64
# Or use online generator: https://generate-random.org/api-key-generator
JWT_SECRET_KEY=GENERATE-A-SECURE-64-CHARACTER-BASE64-ENCODED-SECRET-KEY
JWT_ISSUER=http://localhost:5277/
JWT_AUDIENCE=http://localhost:5277/

# ============================================================================
# External API Keys (REPLACE WITH YOUR ACTUAL KEYS!)
# ============================================================================

# Brevo Email Service (https://app.brevo.com/settings/keys/api)
BREVO_API_KEY=your-brevo-api-key-here
BREVO_SENDER_EMAIL=your-verified-sender-email@domain.com

# Google Services (https://console.cloud.google.com/apis/credentials)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id-here

# OpenAI API (if used)
OPENAI_API_KEY=your-openai-api-key-here

# ============================================================================
# Client Application URLs
# ============================================================================
ADMIN_PANEL_URL=http://localhost:3001

# ============================================================================
# SECURITY CHECKLIST:
# ============================================================================
# ? Replace SA_PASSWORD with a strong password (12+ chars, mixed case, numbers, symbols)
# ? Generate a secure JWT_SECRET_KEY (64+ characters, base64 encoded)
# ? Set real BREVO_API_KEY from Brevo dashboard
# ? Set verified BREVO_SENDER_EMAIL (must be verified in Brevo)
# ? Set real GOOGLE_MAPS_API_KEY with appropriate restrictions
# ? Set real GOOGLE_OAUTH_CLIENT_ID if using OAuth
# ? Ensure .env is in your .gitignore file
# ? NEVER commit .env files to version control
# ============================================================================